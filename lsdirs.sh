#!/bin/bash

# The `-d` option lists directories themselves, rather than the files inside them
# The `-p` option appends a '/' character to the end of directory names
# The `-1` option lists the files and directories one per line

ls -d -p -1 */
